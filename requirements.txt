# Review Buddy Requirements
# Dependencies for paper searching, filtering, and downloading

# Core dependencies (required)
requests>=2.31.0
lxml>=4.9.0
python-dotenv>=1.0.0
beautifulsoup4>=4.12.0
tqdm>=4.66.0

# Bibliography parsing and generation (required)
bibtexparser>=1.4.0
rispy>=0.7.0

# Data processing (required)
pandas>=1.3.0

# Configuration and validation (required for v2.0+)
pydantic>=2.0.0
pyyaml>=6.0

# CLI interface (required for v2.0+)
typer[all]>=0.9.0
rich>=13.0.0

# GUI interface (required for v2.0+)
streamlit>=1.28.0

# Language detection (optional - for abstract filtering)
langdetect>=1.0.9

# Optional dependencies for specific sources
# Google Scholar (optional - install if using Scholar search)
# scholarly>=1.7.0

# Sci-Hub (optional - install if using Sci-Hub downloader)
# scihub

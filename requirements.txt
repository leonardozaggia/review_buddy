# Paper Searcher Requirements
# Minimal dependencies for reliable paper searching

# Core dependencies (required)
requests>=2.31.0
lxml>=4.9.0
python-dotenv>=1.0.0
beautifulsoup4>=4.12.0
tqdm>=4.66.0
pandas>=2.0.0

# Bibliography parsing and generation (required for downloader)
bibtexparser>=1.4.0
rispy>=0.7.0

# Optional dependencies for specific sources
# Google Scholar (optional - install if using Scholar search)
scholarly>=1.7.0

# Sci-Hub (optional - install if using Sci-Hub downloader)
scihub

# Language detection (optional - for abstract filtering)
langdetect>=1.0.9

# For Zotero Translation Server integration (optional):
# Install separately: https://github.com/zotero/translation-server
# Runs as a local Node.js/Docker service on port 1969
# Quick start: docker run -d -p 1969:1969 zotero/translation-server
# See docs/ZOTERO_SETUP.md for detailed instructions
